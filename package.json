{
  "name": "@gastos/expense-ai",
  "version": "0.2.0",
  "description": "Lógica pura compartida de extracción + clasificación de gastos (prompts + config de modelos + parsing + ranking de taxonomía). Single source of truth para gastos-backend y gastos-firebase-functions.",
  "private": true,
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test \"dist/__tests__/*.test.js\"",
    "sync": "node scripts/sync.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
